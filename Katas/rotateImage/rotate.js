const matrix = [
  [1,2,3],
  [4,5,6],
  [7,8,9]
]


function rotate (arr) {
  for (let row = 0; row < arr.length; row++) {
    for(let column = 0; column < arr[row].length; column++)
  }
}

rotate(matrix)